# Backend origin (no trailing slash, without /api).
# Local dev works without this — it falls back to http://localhost:8000.
# On Vercel set it to your Railway backend URL, e.g. https://your-app.up.railway.app
VITE_API_URL=
